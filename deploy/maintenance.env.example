# Copy to /etc/default/solar-charger-maintenance (optional).
# If SCC_ALERT_TO is unset, alert emails are skipped.

SCC_ALERT_TO=
SCC_ALERT_FROM=
# Dashboard Envoy proxy URL (same host as SOLAR_API_BASE + :8080 by default)
SCC_ENVOY_DATA_URL=http://127.0.0.1:8080/api/envoy_data
